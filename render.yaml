services:
  - type: worker
    name: my-worker-bot
    runtime: python
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 main.py
    autoDeploy: true
